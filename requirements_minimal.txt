# Minimal requirements for testing the backend integration
# FastAPI Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0

# AI/ML Dependencies
requests==2.31.0

# Neo4j (optional - we'll use fallback if not available)
neo4j==5.14.1

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6
pydantic==2.5.0

# Skip PostgreSQL for now - we'll use in-memory storage for testing
# psycopg2-binary==2.9.9
# sqlalchemy==2.0.23